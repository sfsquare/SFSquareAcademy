@model IEnumerable<SFSAcademy.PAYROLL_CATEGORY>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NAME)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PCT)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PYRL_CAT_ID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IS_DED)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.STAT)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NAME)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PCT)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PYRL_CAT_ID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IS_DED)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.STAT)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
